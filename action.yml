name: Generate Issues based on input
author: pipo02mix
description: Opens a GitHub issue based on array

inputs:
  token:
    description: GitHub Token
    required: true
  issues:
    description: A JSON array with the issues
    required: true

runs:
  using: node12
  main: dist/index.js

branding:
  icon: alert-octagon
  color: purple
